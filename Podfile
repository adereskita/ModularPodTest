# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ModularPodTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ModularPodTest
  # pod 'ExtensionModule', :path => "../ExtensionModule/"
  pod 'ExtensionModule', :git => "https://github.com/adereskita/ExtensionModuleTest", :branch => "main"

end
